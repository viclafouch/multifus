[package]
name = "paste-probe"
version = "0.0.0"
edition = "2021"
publish = false

# Throwaway. It answers the four questions of the "temps 1" in docs/plan.md and
# is deleted once the answers are written down.
[dependencies]
objc2-core-graphics = { version = "0.3", default-features = false, features = [
  "std",
  "CGEvent",
  "CGEventSource",
  "CGEventTypes",
  # What `CGEventCreateKeyboardEvent` sits behind, and the keycode type with it.
  "CGRemoteOperation"
] }
objc2-core-foundation = { version = "0.3", default-features = false, features = [
  "std"
] }
